<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".VerPerfilPrestador">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp"
        android:gravity="center_horizontal">

        <!-- 🧑‍🔧 Foto del prestador -->
        <ImageView
            android:id="@+id/imgPerfilPrestador"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:layout_marginTop="20dp"
            android:src="@drawable/usuario"
        android:scaleType="centerCrop"
        android:contentDescription="Foto del prestador" />

        <!-- 📛 Nombre -->
        <TextView
            android:id="@+id/tvNombrePrestador"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Juan Pérez"
            android:textStyle="bold"
            android:textSize="22sp"
            android:textColor="#000000"
            android:layout_marginTop="12dp" />

        <!-- 💼 Tipo de trabajo -->
        <TextView
            android:id="@+id/tvTrabajoPrestador"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Electricista certificado"
            android:textSize="16sp"
            android:textColor="#555555"
            android:layout_marginTop="4dp" />

        <!-- 📍 Ubicación -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="8dp">

            <ImageView
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@android:drawable/ic_menu_mylocation"
                app:tint="@color/teal_700"
                android:layout_marginEnd="4dp" />

            <TextView
                android:id="@+id/tvUbicacionPrestador"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Bogotá, Colombia"
                android:textColor="#666666" />
        </LinearLayout>

        <!-- 📞 Teléfono -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="6dp">

            <ImageView
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@android:drawable/ic_menu_call"
                app:tint="@color/teal_700"
                android:layout_marginEnd="4dp" />

            <TextView
                android:id="@+id/tvTelefonoPrestador"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="+57 300 123 4567"
                android:textColor="#666666" />
        </LinearLayout>



        <!-- 📝 Descripción -->
        <TextView
            android:id="@+id/tvDescripcionPrestador"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Soy un profesional con más de 5 años de experiencia en instalaciones eléctricas, mantenimiento y emergencias domiciliarias."
            android:textSize="15sp"
            android:textColor="#444444"
            android:layout_marginTop="16dp"
            android:lineSpacingExtra="4dp" />

        <!-- 🔘 Botón Solicitar -->
        <Button
            android:id="@+id/btnSolicitarServicio"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Solicitar Servicio"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:layout_marginTop="24dp"
            android:backgroundTint="@color/teal_700"
            android:paddingVertical="12dp" />

    </LinearLayout>
</ScrollView>
